name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  run-tests:
    runs-on: buildinator-5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run test script
        run: ./test_builds.sh
